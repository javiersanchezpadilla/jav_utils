""" {}.clear()

    Elimina TODOS los elementos del diccionario, no retorna nada

"""

a = {"x":1, "y":2}
                    # Modifica el diccionario, pero no retorna o regresa 
                    # ningún valor, por lo tanto b = None

b = a.clear()     

print(f'b = {b}')
print(f'a = {a}')
